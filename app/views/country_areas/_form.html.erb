<%= form_for(@country_area) do |f| %>
  <% if @country_area.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@country_area.errors.count, "error") %> prohibited this country_area from being saved:</h2>

      <ul>
      <% @country_area.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
